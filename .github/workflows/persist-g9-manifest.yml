name: persist Grade-9 generated manifest

on:
  push:
    branches:
      - tmp/g9-hardening-manifest-preview

permissions:
  contents: write

jobs:
  persist:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install --quiet jsonschema
      - name: Generate repository-native artifacts
        run: python3 Shared/tools/build_manifest.py
      - name: Commit manifest if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/architecture-manifest.json
          if git diff --cached --quiet; then
            echo "manifest already current"
            exit 0
          fi
          git commit -m "CI helper: persist Grade-9 manifest"
          git push origin HEAD:"${GITHUB_REF_NAME}"
