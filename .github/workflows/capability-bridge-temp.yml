name: capability bridge temporary artifact refresh

on:
  push:
    branches:
      - core/capability-delivery-bridges

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: core/capability-delivery-bridges
          fetch-depth: 0

      - name: Remove temporary workflow before generation
        run: rm .github/workflows/capability-bridge-temp.yml

      - name: Regenerate committed derived artifacts
        run: python3 Shared/tools/build_manifest.py

      - name: Commit refreshed artifacts and self-remove
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/architecture-manifest.json tools/data.js .github/workflows/capability-bridge-temp.yml
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "Regenerate capability bridge artifacts"
          git push origin HEAD:core/capability-delivery-bridges
